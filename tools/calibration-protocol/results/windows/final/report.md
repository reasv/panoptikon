# Windows final regression — the merged tip 2133563f on the dual-5090 desktop

Host `A-DESKTOP`, Windows 11 (10.0.26200), 2 × NVIDIA GeForce RTX 5090 (sm_120, 32 607 MiB each),
driver 610.74, torch 2.7.1+cu128, rustc stable-x86_64-pc-windows-msvc. Tip **2133563f**
(`claude/batch-calibration-coverage-db9ab9`, every fix of the day merged), clone
`Q:\projects\panoptikon-pr27` fetched from a bundle onto branch `final-tip`, ports 6382/6383/6379.
The user's own instance on `Q:\projects\panoptikon` (6339/6342/6343) ran throughout and was not
touched. 2026-09-17, 00:52 – 02:38 UTC.

Comparands: **run4 §3** (the regression pass on 0d7f5671), **run4 §2** (the seeded 32 GB legs — the
section the coordinator's addition asked for), and **run5-toobig-2** (the 8B refusal and
condemnation, at 73765a8f).

## 0. Build, venv, selftest

* `cargo build --release -p panoptikon` on the merged tip: **succeeded, 1 m 33 s**. **No D0** —
  §4.22's Windows build defect stays fixed. Warnings went **1 → 8**: the pre-existing
  `dead_code` on `rlimit.rs`'s `NofileRaise` variants, plus **seven new ones, all in
  `panoptikon/src/inferio/cpu.rs`** (`cgroup_limit_mb`, `cgroup_used_mb`, `bytes_file_mb`,
  `file_lru_mb`, `FILE_LRU_V1`, `FILE_LRU_V2`, `MIB`). They are the cgroup readers added by
  `ab627903` / `418cfe1a`: their only callers sit inside `#[cfg(target_os = "linux")]` blocks
  (`cpu.rs:182`, `cpu.rs:207`) while the functions themselves carry no `cfg`, so every non-Linux
  build reports them dead. Cosmetic, but it is new noise on two of the three platforms — see W-F5.
* `uv sync --locked --extra cu128 --group test --directory python`: rc 0, `Resolved 138 packages`,
  `Audited 99 packages` — **nothing moved**; the venv already matched the lock from the run5b
  session. One mechanics note for the next pass: the **scoop shim** `C:\Users\moot\scoop\shims\uv.exe`
  exits 1 with no output at all under a non-interactive ssh session (the same class of breakage as
  the 0-byte `~/.cargo/bin` shims run4 §0 records); the versioned binary
  `C:\Users\moot\scoop\apps\uv\0.6.13\uv.exe` works.
* `python -m pytest tools/calibration-protocol/tests -q`: **203 passed, 2 skipped** (run4: 165
  passed, 2 skipped — 38 new tests on this tip).
* `selftest.py` (`selftest.log`, `win-selftest.json`), unchanged from run4 in every line that
  matters:

  ```
  free_source  nvml     27413 / 32607 MiB      (torch tier reads 30577 / 32606: over-reports, as in run4)
  base_mb      841      base_method free_delta (nvml tier: "NVML lists no process with this pid")
  batch of 8   366.3 ms, peak_allocated 609 MiB, peak_reserved 718 MiB
  VERDICT: degraded - base:free_delta; oracle:no NVML per-process figure
  ```

  run4 read `27137 / 32607` and `base_mb 840`. **The known WDDM residues therefore stand,
  named and by design**: `oracle_agreement`, `base_accuracy` and `footprint_agreement` SKIP on
  every leg below, because on WDDM neither NVML nor `nvidia-smi --query-compute-apps` attributes
  a byte to a PID. That is a property of the platform, not a verdict about this tip.
* **The corpora had to be regenerated, and one of them could not be.** `00f22c55` stamps a
  generator version into `manifest.json` and makes a leg refuse a corpus generated before it, so
  every run4 corpus was rejected (`generated by corpus.py version unstamped, this leg needs 2`).
  Regenerating is cheap and deterministic — `corpus.py` changed by +8 lines, the stamp itself —
  and `ramp` (2 000), `text` (2 300) and both smoke tiers came back with the same content the legs
  actually index: every leg below rescans **180** files out of a smoke tier and **300** scanned
  pages out of the text tier, the same counts run4's legs recorded. The one difference is that
  the five `pdf-4p` items fail to generate on this host, so both smoke tiers hold 200 items where
  run4's `smoke` held 205 — the same five were already missing from run4's `smoke2`, and no leg
  indexes a PDF. **`ramp8` cannot be
  regenerated at all**: the scenario field names a *directory* convention (`ramp8`) while the
  manifest records the *tier* `corpus.py` was given (`ramp` at `--scale 8`), and `corpus.py` has
  no `ramp8` tier to offer, so the guard refuses S4b, S4c and S4d for ever. Worked around here by
  writing `"tier": "ramp8"` into that one manifest, over content that is exactly the 8 × ramp the
  three scenarios ask for. See **W-F4** — it blocks those three scenarios on every platform.

## 1. The shipped Windows copies are in the binary under test

`55842fc1` / `9a9250e4` allowlist the Windows copy for the 80 measured-equal item- and
pixel-priced ids, and the shipped sm_120 baseline carries them as real rows. Counted in the
checkout under test:

| | profiles in the file | `platform = "windows"` | `platform = "linux"` |
|---|---|---|---|
| `0d7f5671` (run4's tip) | 88 | **1** (`tags/wd-vit-tagger-v3`) | 87 |
| `2133563f` (this tip) | 168 | **81** | 87 |

81, not 80: the 80 new copies plus the one wd-vit row run4 already had. (Count windows
rows with the exact string — every copy also carries `base_platform = "linux"`, so a
substring match on `platform = "linux"` returns 168 and not 87.) Each copy is otherwise the
linux row verbatim. **Seeding therefore no longer needs run4's
rewritten `seed-sm120-win.toml`** — no leg below passes `--seed-calibration`, and every "seeded"
leg in this report is a *fresh store on a clean root* that the shipped half fills. That is the
route the 2026-09-07 ruling describes and the one a user actually gets.

Four rows matter below and are quoted here so the numbers in §3, §4 and §4b can be read
against them:

| id | base_mb | slope_mb_per_unit | knee_units | max_units_measured |
|---|---|---|---|---|
| `tags/wd-vit-tagger-v3` | 964 | 29.865328820116055 | – | 192 |
| `clip/ViT-H-14-quickgelu_dfn5b` | 2588 | 10.331845238095237 | **63** | 192 |
| `clip/qwen3-vl-embedding-8b` | **31752** | 0.00034207525655644244 | 4194303 | – |
| `doctr/db_resnet50_crnn_vgg16_bn` | 858 | **8.0** | – | 128 |

## 2. Regression pass, leg by leg, against run4 §3

### The comparison is against run4 **re-scored on this tip's analyzer**

`analyze.py` moved three commits since `0d7f5671` (`c1c5197d`, `00f22c55`, `0a337893`), so
run4's 18 regression recordings were re-scored on the box with the tip's `analyze.py` before
anything was compared (`.win/run4/legs/<leg>/<scen>/verdicts-tip.json`, kept here as
`run4-tip-verdicts.json`). The result is the cleanest possible control:

> **0 analyzer-induced moves.** All 18 legs × 18 checks are identical under both analyzers.

So every cell that moves below moved because the run moved, not because the scorer did.

### The table

Nineteen legs: run4's eighteen plus `f-S2-unseeded`, which has no run4 comparand and is
marked `(n)`. `-` is a check the scenario does not ask for.

| leg | oracle | base | footprint | slope | grant | failures | deflation | idle | util | thru | persist | job | ledger | fds | hog | ramp | learned | retries |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `f-S1` | SKIP | SKIP | INFO | - | PASS | PASS | - | - | - | - | - | PASS | PASS | INFO | - | - | - | - |
| `f-S2` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | **INFO→PASS** | PASS | PASS | INFO | SKIP | INFO | PASS | PASS |
| `f-S2-unseeded` | SKIP (n) | SKIP (n) | INFO (n) | SKIP (n) | PASS (n) | PASS (n) | PASS (n) | PASS (n) | SKIP (n) | INFO (n) | PASS (n) | PASS (n) | PASS (n) | INFO (n) | SKIP (n) | INFO (n) | PASS (n) | PASS (n) |
| `f-S3` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | PASS | PASS | PASS | INFO | SKIP | INFO | INFO | PASS |
| `f-S4a` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | INFO | PASS | WARN | INFO | INFO | INFO | INFO | PASS |
| `f-S4b` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | INFO | PASS | PASS | INFO | INFO | INFO | INFO | PASS |
| `f-S4c` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | INFO | PASS | **PASS→WARN** | INFO | INFO | INFO | INFO | PASS |
| `f-S4d` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | INFO | PASS | WARN | INFO | INFO | INFO | INFO | PASS |
| `f-S5-dies_on_load` | SKIP | SKIP | SKIP | WARN | SKIP | PASS | SKIP | PASS | WARN | INFO | WARN | FAIL | PASS | INFO | SKIP | SKIP | INFO | SKIP |
| `f-S5-dying` | SKIP | SKIP | INFO | WARN | PASS | FAIL | PASS | PASS | SKIP | INFO | WARN | FAIL | PASS | INFO | SKIP | INFO | INFO | SKIP |
| `f-S5-failbatch` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | PASS | PASS | WARN | INFO | SKIP | INFO | INFO | PASS |
| `f-S5-failbatch_oomtext` | SKIP | SKIP | INFO | SKIP | PASS | PASS | PASS | PASS | SKIP | INFO | PASS | PASS | WARN | INFO | SKIP | INFO | INFO | PASS |
| `f-S5-oom_second_batch` | SKIP | SKIP | INFO | SKIP | PASS | WARN | SKIP | PASS | WARN | INFO | **FAIL→PASS** | PASS | PASS | INFO | SKIP | SKIP | INFO | PASS |
| `f-S14-tags` | - | - | - | - | PASS | PASS | - | - | - | - | - | PASS | - | INFO | - | - | - | - |
| `f-S14-clip` | - | - | - | - | PASS | PASS | - | - | - | - | - | PASS | - | INFO | - | - | - | - |
| `f-S14-ocr` | - | - | - | - | PASS | PASS | - | - | - | - | - | PASS | - | INFO | - | - | - | - |
| `f-S14-whisper` | - | - | - | - | SKIP | PASS | - | - | - | - | - | PASS | - | INFO | - | - | - | - |
| `f-S14-florence2` | - | - | - | - | PASS | PASS | - | - | - | - | - | PASS | - | INFO | - | - | - | - |
| `f-S14-textembed` | - | - | - | - | PASS | PASS | - | - | - | - | - | PASS | - | INFO | - | - | - | - |

**No new FAIL.** The three FAIL cells (`f-S5-dies_on_load` / `f-S5-dying` `job_outcome`,
`f-S5-dying` `failures`) are the same three run4 had, for the same reason: the fault-injection
fixtures are scored with `legs.py`'s own default flags, which do not pass `--expect-deaths` /
`--expect-failed-items`, so a fixture that is *meant* to kill its worker reads as a failure.

### The three moves, with numbers

| move | leg | numbers | reading |
|---|---|---|---|
| `persistence` INFO → **PASS** | `f-S2` | run4 `queued 4, writes 3, no anchor_advanced update inside the recording`; now `queued 5, writes 3, worst anchor-advance → store-write delay **24.7 s** [threshold 30 s]` | The same three store writes on both sides and one profile at the end. What changed is that an `anchor_advanced` update fell *inside* the recording window this time, so the check had something to time instead of reporting that it did not. A window artefact, and it moves the right way |
| `ledger_invariant` PASS → **WARN** | `f-S4c` | run4 `0 of 924 GPU-samples, 0 over_grant, 0 limit_fell, 10 grants`; now `**22 of 2781** GPU-samples, **0 over_grant**, 22 `limit_fell`, 20 grants, 0 of 20 grants over headroom` | The 2 GB spike takes the limit below a footprint already held — `limit_fell`, never `over_grant`. It is a sampling-density effect, not a new behaviour: the same class is counted **92** times in `f-S4a` (run4: 62), **41** in `f-S4d` (run4: **338**), 25 and 22 in the two failbatch legs (run4: 2 and 2), all at 0 `over_grant`. Only S4c crossed the 0 → non-zero boundary the verdict is keyed on. See W-F7 |
| `persistence` FAIL → **PASS** | `f-S5-oom_second_batch` | both sides print `worst anchor-advance → store-write delay **30.0 s** [threshold 30 s]`, 2 writes, 1 profile | The identical printed number lands on either side of the threshold: run4's true delay was a hair over 30 s, this run's a hair under. Exactly the boundary artefact run4 recorded as **W-A7**, on a leg whose job ran 0.083 s. The job itself is `completed`, 180/180, 0 errors on both sides |

### Throughput, the ramp and the fit

`items/s = total_segments / inference_time` from `jobs.json` — the run4/run5 basis, not
`analyze.py`'s `throughput` line.

| leg | items | this run items/s | run4 items/s | this run slope / anchor / knee | run4 slope / anchor / knee |
|---|---|---|---|---|---|
| S2 | 2 000 | 47.018 | 48.780 | 29.8588 / **256** / – | 29.8594 / 512 / – |
| S2 **unseeded** | 2 000 | 45.536 | – | **30.0** / **16** / **7** | – |
| S3 | 2 000 | 46.422 | 48.780 | 29.8594 / 256 / – | 29.8601 / 512 / – |
| S4a | 2 000 | 42.056 | 47.619 | 29.8660 / **56** / – | 29.8571 / 55 / – |
| S4b | 16 000 | 47.434 | 50.314 | 29.8622 / 256 / – | 29.8594 / 512 / 255 |
| S4c | 16 000 | 45.887 | 50.000 | 29.8584 / **512** / **255** | 29.8594 / 587 / 255 |
| S4d | 16 000 | 43.803 | 48.930 | 29.8598 / **567** / 15 | 29.8593 / 521 / – |

Every fitted slope is inside **0.03 %** of run4's and of the shipped row (0.99977–1.00002 of
29.865328820116055). Throughput is 3–12 % below run4 on the same host and the same GPU 0 that
the user's desktop and instance share — see **W-F6**; nothing else about these legs moved.
`alloc_retries` is PASS everywhere it applies, with **0** retries on every leg except S4c (1 of
20 windows) and S4d (1 of 274) — the same two legs, the same order of magnitude, as run4.

### Seeded against unseeded, on the one id the shipped file has always carried

`f-S2` and `f-S2-unseeded` are the same scenario, the same corpus and the same binary; the only
difference is that the second ran with `tags/wd-vit-tagger-v3`'s Windows row removed from the
shipped baseline (80 windows rows in the file instead of 81, restored afterwards — see §6).

| | `f-S2` (shipped copy present) | `f-S2-unseeded` |
|---|---|---|
| `seeded calibration from a stored profile` | **yes**, `local=false fit_is_local=false slope=29.865328820116055 samples=5 max_units_measured=192` | **none at all** |
| grants to drain 2 000 items | **6** | **75** |
| largest unit budget | **256** | **16** |
| fitted knee | none | **7 units** |
| `max_units_measured` reached | 256 | 16 |
| fitted slope | 29.8588 | 30.0 (8 samples, 221 local) |
| items/s | 47.018 | 45.536 |
| OOM / deflation / collapse | 0 / 0 / 0 | 0 / 0 / 0 |

Both drain 2 000/2 000 with zero failures, so this is not a safety difference — it is the ramp.
Without the copy the cold ramp needs **twelve times** as many windows, tops out at **16** units,
and re-fits the **knee at 7** that §4.22 blamed for this host's low throughput and that run4
reported as gone. It is not gone: the shipped Windows copy is what keeps it away. See **W-F3**.

### T7 stays closed, and the token row stays untravelled

`f-S14-textembed` ran the OCR → embed chain end to end again: `doctr/…crnn_mobilenet_v3_small`
300/300 then `textembed/all-MiniLM-L6-v2`, `grant_safety` PASS, `job_outcome` PASS. The doctr
side was **seeded from the shipped copy** (`local=false`, slope 8.0). The textembed side fitted
**0.014876 MiB/token** (18 samples, `max_units_measured` 4 669) against run4's **0.014708**
through the same chain, run4's **0.018821** through the loadgen text route, and Linux's
**0.016302** — 0.9125 of the Linux row from one route and 1.154 from the other. run4's **W-A2**
stands unchanged: no `aggregation = "max-times-count"` id may carry a `platform_copies` copy,
and none does.

## 3. The seeded 32 GB set, against run4 §2

run4 §2 seeded these legs from `seed-sm120-win.toml`, a hand-rewritten local copy of three
shipped rows with `platform` flipped, and ran each id twice — once "fresh" (only wd-vit had a
shipped Windows row) and once with the local seed. **On this tip neither is needed.** Every leg
below is a *fresh store on a clean root*; the shipped Windows copies do all the seeding, and the
log line proves which half they came from:

```
seeded calibration from a stored profile model=tags/wd-vit-tagger-v3      local=false fit_is_local=false
    slope_mb_per_unit=29.865328820116055 samples=5 local_samples=0 max_units_measured=192
seeded calibration from a stored profile model=clip/ViT-H-14-quickgelu_dfn5b local=false fit_is_local=false
    slope_mb_per_unit=10.331845238095237 samples=13 local_samples=0 max_units_measured=192
```

`local=false` is the shipped half. run4 needed both an `A-…-fresh` and an `A-…-seed` leg to show
the 2026-09-07 ruling (any matching profile confers its anchor, local or shipped); `f-S3` shows
it inside one leg, because its second job after the restart re-seeds from the **local** row it
just wrote — `local=true fit_is_local=true confirms=true slope=29.859437751004016 samples=7
max_units_measured=256` beside the shipped `local=false … samples=5 … 192` of its first job. Same mechanism, same anchor semantics.

All six legs ran on **GPU 1** (`server-C1win-g1.toml` + `env.C1win-g1`), hog on GPU 1
(`--hog-device 1`), forced with `--min-free-mb 12288` / `8192`; S4a's own fraction scales to
4 093 MiB on this board, so the floor binds and each leg records `floor_bound {"resolved_mb":
12288 | 8192}`, exactly as run4.

| leg | seeded from | conferred anchor / knee | 1st window (units, MiB) | largest window (units, MiB) vs share / headroom | min headroom | defl | OOM | collapse | over_grant | items | items/s | run4 items/s (fresh / seed) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `f-wdvit-S2` | shipped copy | 192 / – | 1, 38 | **512**, 24 179 vs 30 277 / 18 175 | 5 098 | 0 | 0 | 0 | 0 | 2000/2000 | 47.718 | 55.19 / 55.20 |
| `f-wdvit-S4a12` | shipped copy | 192 / – | 3, 112 | **256**, 9 663 vs 10 268 / 5 426 | 0 | 0 | 0 | 0 | 0 | 2000/2000 | 51.453 | 55.73 / 55.74 |
| `f-wdvit-S4a8` | shipped copy | 192 / – | 1, 38 | **166**, 6 140 vs 6 172 / 1 428 | 0 | 0 | 0 | 0 | 0 | 2000/2000 | 47.195 | 54.12 / 54.28 |
| `f-vith14-S2` | shipped copy | 192 / **63** | 2, 26 | **127**, 1 751 vs 28 476 / 27 594 | 26 394 | 0 | 0 | 0 | 0 | 2000/2000 | 101.992 | 108.61 / 111.04 |
| `f-vith14-S4a12` | shipped copy | 192 / 63 | 1, **13** | **127**, 1 755 vs 8 618 / 7 734 | 6 532 | 0 | 0 | 0 | 0 | 2000/2000 | 97.263 | 110.49 / 111.16 |
| `f-vith14-S4a8` | shipped copy | 192 / 63 | 1, **13** | **127**, 1 755 vs 4 520 / 3 636 | 2 436 | 0 | 0 | 0 | 0 | 2000/2000 | 97.155 | 110.21 / 110.91 |

**Every claim run4 §2 made reproduces, from the shipped rows alone.**

* **A big-card anchor never set the budget on a small card.** The conferred anchor is 192 on all
  six legs. The budget the card actually ran was **512 / 256 / 166** for wd-vit as free memory
  went 30 GB → 12 GB → 8 GB — exceeded where memory allowed, undercut where it did not, and
  within one unit of run4's **512 / 256 / 165**. It is a ratchet claim, not a budget: the MiB
  charged tracks `share_mb` every window.
* **The conferred knee, not memory, fixes ViT-H-14.** Seeded, the first window is priced at
  **13 MiB for 1 unit** from the conferred slope, and the budget settles at **127 units /
  1 751–1 755 MiB — identical at 30 GB, 12 GB and 8 GB free**, exactly run4's number, fitting inside
  the 4 520 MiB share at the tightest level with 3 636 MiB of headroom left. run4 needed an
  unseeded contrast leg to show what the alternative costs (one window priced at the whole
  share, 45 windows to reach 32 units); this tip has a shipped row for the id, so the unseeded
  path is no longer reachable without deleting it, and §4's 8B leg is where that is done.
* **Zero OOM, zero throughput collapse, zero `over_grant`, zero deflation and 2 000/2 000 items
  on all six legs**, at every memory level, with `alloc_retries` PASS at 0 retries on all six.
* Every fitted slope is within **0.3 %** of its shipped row: wd-vit 0.99974–0.99983, ViT-H-14
  0.99721–1.00069.

The one number that is not run4's is throughput: 47–51 items/s for wd-vit against run4's 54–56,
and 97–102 for ViT-H-14 against run4's 109–111 — a uniform **9–12 % shortfall with the budgets,
anchors and slopes unchanged**, which is what a host-side (CPU preprocessing) difference looks
like and not a pricing one. **W-F6.**

## 4. The 8B embedder: seeded refusal and unseeded condemnation, against run5-toobig-2

Both legs are `S2` on **GPU 1** with `clip/qwen3-vl-embedding-8b` (~30.7 GiB of weights) on a
32 607 MiB board, `--job-cap 480`. The only difference is the shipped row:

* **seeded** — the file as shipped, **81** windows rows, including
  `clip/qwen3-vl-embedding-8b base_mb = 31752`;
* **unseeded** — that one profile block stripped, **80** windows rows, everything else byte
  identical, restored immediately afterwards (§6).

| | `f-qwen8b-S2-seeded` | `f-qwen8b-S2-unseeded` | run5-toobig-2 T1 / T2 | run4 §2 (0d7f5671) |
|---|---|---|---|---|
| outcome | **load refused** | **condemned, then fatal** | refused / condemned | **OOM storm** |
| grants | **0** | 22 (all pre-fit, 1 memory-blind) | 0 / 22 | 22 |
| deflation reached | – | **21** | – / 21 | 22 |
| `torch.OutOfMemoryError` lines | **0** | **94** | 0 / **176** | **5 018** and **6 254** |
| worker deaths | 0 | **1** | 0 / 1 | 0 (it never stopped) |
| items attempted / failed | 0 / 0 | 23 / 23 | 0 / 22 | 1 124 and 1 436 failed, **0 completed** |
| job outcome | `failed` | `failed` | failed / failed | **`running`** to the 480 s cap |
| wall clock of the leg | 54 s | 89 s | 4 s / 38 s | 480 s (the cap) |
| store row written | none | none | none | none |

### Seeded: refused in seconds, and the sentence reaches `/health` and the job

```
ERROR panoptikon::inferio::http: failed to load model model=clip/qwen3-vl-embedding-8b
  error=failed to load model clip/qwen3-vl-embedding-8b: model clip/qwen3-vl-embedding-8b
  needs about 31752 MiB on GPU GPU-c77d0f94-…, which has room for 31083 MiB; not loading it
```

`/api/inference/health.load_cooldowns`: `failures = 1`, `window_secs = 2`, `last_error` = that
sentence verbatim. `jobs.json.failure_reason`:

```
Failed to load model: model clip/qwen3-vl-embedding-8b failed to load on all 1 inference
endpoints: inference request failed (500): Failed to load model: failed to load model
clip/qwen3-vl-embedding-8b: model … needs about 31752 MiB on GPU GPU-c77d0f94-…, which has
room for 31083 MiB; not loading it
```

**0 grants, 0 OOM lines, 0 items attempted.** What is new against run5-toobig-2's T1 is the
route: T1 was seeded from run5's hand-written `seed-qwen8b-win.toml`, and this leg is seeded by
the **shipped baseline on a fresh store** — the thing a user actually has. 31752 > 31083 refuses.

### Unseeded: condemned inside one deflation ladder, and 94 OOM lines, not 5 018

```
ledger: admitted a worker to a GPU's ledger model=clip/qwen3-vl-embedding-8b replica=2
        base_mb=Some(31173) base_method="free_delta" reserved_at_load_mb=Some(31202)
        seeded_from_store=false
ledger: issued a memory grant … unit_budget=1 mb=346 room_mb=346 headroom_mb=0
        external_mb=870 reserve_mb=218 pre_fit=true ramp_step=0 deflation=21
ledger: this model cannot run a single item on this GPU; failing it instead of dispatching
        to it again model=clip/qwen3-vl-embedding-8b gpu=GPU-c77d0f94-… base_mb=31173
        room_mb=31650 windows=3
manager: worker died fatally; dropping model from all caches cooldown_secs=2.0
```

One memory-blind window, then 21 priced one-item windows while the deflation ladder walks the
unit budget down to 1; the rule counts a window only at `unit_budget <= 1`, so the three that
condemn are the blind first window and the last two. 22 grants, one death, and the queue fails
**once**: 23 items attempted, 23 failed, `13.4 s` of inference time in an 89 s leg. `base_method`
came back **`free_delta`** here, not run5-toobig-2's `alloc_delta`.

**`fde87cad`'s floor rule is visible in one number.** The condemnation sentence now ends with a
clause run5-toobig-2 did not have:

```
model clip/qwen3-vl-embedding-8b ran out of memory on GPU GPU-c77d0f94-… at a one-item batch
3 windows running: its base is 31173 MiB of the 31650 MiB this GPU can lend, and one item on
top of it did not fit; the next load of it here is refused under 31520 MiB of room
```

— the remembered working set, stated per card, in the sentence the user sees, and bounded:
**31 520 = 31 173 + 347**, i.e. base plus the *bounded pre-fit one-item price* (the failing
window was granted `mb=346 room_mb=346`), not base plus the whole card. Under run5-toobig-2's
W1 arithmetic the same leg's remembered set would have been the failing window's whole room on
top of the base; the doc's "reaches the card's room in at most two cycles" is what this bound
buys. The same sentence is what `/health.load_cooldowns.last_error` and
`jobs.json.failure_reason` carry.

**The OOM count is the headline.** 94 `torch.OutOfMemoryError` lines (192 lines containing
`CUDA out of memory`, counting the fall-back-to-per-request retries), against
**run5-toobig-2's 176**, run5 round 1's **8 002**, and run4's **5 018 / 6 254 with the job never
even ending**. `alloc_retries` reports 26 allocator retries over 22 windows — the deflation
ladder doing its job. There is no storm left: the cost of the fix's shape is still one ladder,
21 windows, ~13 s, and that is all.

`job_outcome` FAILs on both legs, correctly and for a new reason: `00f22c55` makes a job that
ran on **0 items** a FAIL rather than a green leg, and a refused or condemned model is exactly
that. The refusal legs are the check's intended catch.

## 4b. The per-implementation spot check: a shipped Windows copy is *used*, and it is right

The dedicated leg is `copy-usage/copycheck-doctr`: one gateway on GPU 1, a **fresh store**,
`lru_size = 1`, `doctr/db_resnet50_crnn_vgg16_bn` driven by `loadgen.py` from four concurrent
slots at `items=` 5 / 19 / 61 / 151 for 600 s — run4's `B4-doctr-r2` recipe, the one that
satisfies **W-A3** (the ring must reach the Linux row's `max_units_measured`).

```
DEBUG panoptikon::inferio::ledger: seeded calibration from a stored profile
  model=doctr/db_resnet50_crnn_vgg16_bn gpu=GPU-c77d0f94-…
  local=false fit_is_local=false exact_torch=true confirms=false
  slope_mb_per_unit=8.0 samples=8 local_samples=0 max_units_measured=128
```

`local=false` is the shipped half: this is `9a9250e4`'s Windows copy of the sm_120 row, being
conferred on a store that has never seen the model. The leg then ran 424 requests / 24 870
items at 40.74 items/s (`loadgen.log`), 106 grants, a budget reaching 128 units, **0 OOM**, and
wrote its own fit:

| | shipped Windows copy | this leg's fit |
|---|---|---|
| `base_mb` | 858 | 737 (`free_delta` — the documented Windows offset, not a slope difference) |
| `slope_mb_per_unit` | **8.0** | **8.036363636363637** (ratio **1.00455**) |
| `samples` / `local_samples` | 8 / – | 6 / **261** |
| `max_units_measured` | 128 | 128 |
| `residual_mb` | – | 2.58 |

And the same evidence falls out of eighteen more legs for free, because every one of them
started from a fresh store and every one was seeded by a shipped copy. Six ids across four
implementation families, each with the leg's own local fit beside the row it was seeded from:

| leg | id | shipped copy's slope (`local=false`) | leg's own fit | ratio | fit samples |
|---|---|---|---|---|---|
| `copycheck-doctr` | `doctr/db_resnet50_crnn_vgg16_bn` | 8.0 | 8.036363636363637 | 1.00455 | 6 |
| `f-S14-ocr` | `doctr/db_resnet50_crnn_mobilenet_v3_small` | 8.0 | **8.0** | **1.00000** | 3 |
| `f-S14-textembed` | `doctr/db_resnet50_crnn_mobilenet_v3_small` | 8.0 | 8.835020622422197 | 1.10438 | 4 |
| `f-S14-clip` | `clip/apple_MobileCLIP-S1` | 8.371347745313292 | 8.435897435897436 | 1.00771 | 3 |
| `f-S14-florence2` | `florence2/msft_large-caption` | 382.56904761904764 | 383.5 | 1.00243 | 4 |
| `f-vith14-S2` / `S4a12` / `S4a8` | `clip/ViT-H-14-quickgelu_dfn5b` | 10.331845238095237 | 10.3030 / 10.3333 / 10.3390 | 0.99721 / 1.00014 / 1.00069 | 6 / 6 / 6 |
| `f-S1` | `tags/wd-vit-tagger-v3` | 29.865328820116055 | 29.85221078134464 | 0.99956 | 4 |
| `f-S2` / `S3` / `S4a` / `S4b` / `S4c` / `S4d` | `tags/wd-vit-tagger-v3` | 29.865328820116055 | 29.8588 / 29.8594 / 29.8660 / 29.8622 / 29.8584 / 29.8598 | 0.99977 – 1.00002 | 6–23 |
| `f-S14-tags` | `tags/wd-vit-tagger-v3` | 29.865328820116055 | 29.862647209421404 | 0.99991 | 4 |
| `f-wdvit-S2` / `S4a12` / `S4a8` | `tags/wd-vit-tagger-v3` | 29.865328820116055 | 29.8591 / 29.8574 / 29.8601 | 0.99974 – 0.99983 | 7 / 8 / 8 |

**Nineteen of the pass's twenty-eight legs logged `local=false`**; the nine that did not are
the five fault-injection fixtures and `f-S14-whisper` (no shipped row exists for any of them),
the two deliberately unseeded legs, and the seeded 8B leg, which is refused before a store is
ever consulted. Where the ring had room to move — the long legs,
6 to 23 samples — the fit lands inside **0.5 %** of the shipped copy and usually inside 0.03 %.
The one row above 1.01 is `f-S14-textembed`'s doctr side at **1.104 on 4 samples** over a
300-item chain leg, which is **W-A3** restated, not a bad copy: the same id in `f-S14-ocr`,
on the same tip and the same hour, fits **8.0 exactly**. `florence2/msft_large-caption` is
worth naming separately: 383.5 against the shipped 382.569 is a **1.00243** Windows
confirmation of the one family run4 §1 left with an unresolved objection — the copy key cannot
express "only while `config.flash_attention` is false". The number travels; the objection is
unchanged by this pass.

## 5. Findings

| # | severity | finding |
|---|---|---|
| **W-F1** | none — **closes run4's W-A1** | **A model that does not fit is refused or condemned, never an OOM storm.** `clip/qwen3-vl-embedding-8b` on a 32 607 MiB card: seeded from the **shipped** Windows row it is refused in seconds with `needs about 31752 MiB … room for 31083 MiB; not loading it`, **0 grants, 0 OOM lines**, and that sentence in `/health.load_cooldowns.last_error` and in `jobs.json.failure_reason`. Unseeded it is condemned after one deflation ladder (22 grants, deflation 21, 3 counted one-item windows), **94** `torch.OutOfMemoryError` lines, one worker death, 23 items failed, 89 s. run4 on the same model and the same card: **5 018 and 6 254** OOM lines, 0 completed, job `running` to the 480 s cap. run5-toobig-2: 0 / **176** |
| **W-F2** | none — the change under test works | **The 80 shipped Windows copies are used, on a fresh store, and they are right.** **19 of the 28 legs** logged `seeded calibration from a stored profile … local=false`, over **6 ids in 4 implementation families** — `wd_tagger` (wd-vit), `doctr` (both `db_resnet50_crnn_*` ids), `openclip` (MobileCLIP-S1 and ViT-H-14) and `florence2` (`msft_large-caption`). A seventh copy, the 8B `clip/qwen3-vl-embedding-8b` row, is used on the other path entirely: it is what lets §4's seeded leg refuse the load before a worker exists. Every long leg's own fit lands inside 0.5 % of the copy it was seeded from, most inside 0.03 %. run4 needed a hand-written `seed-sm120-win.toml`; **no leg in this report passes `--seed-calibration`** |
| **W-F3** | low, observation | **The low knee is not gone — the shipped copy is what hides it.** `f-S2-unseeded` (the wd-vit Windows row removed, everything else identical to `f-S2`) fits a **knee at 7 units**, tops out at a **16**-unit budget, and needs **75** grants to drain the same 2 000 items that `f-S2` drains in **6** at a 256-unit budget. run4 §3 read the disappearance of pass 2's knees at 3 and 7 as `fix/knee-race` landing; on this host it is at least as much the shipped row starting the ramp above the rung the ring fails to certify. Nothing unsafe happens either way (0 OOM, 0 deflation, 2 000/2 000 on both), and the throughput cost is small here (45.5 vs 47.0 items/s) — but a platform with no copy still walks the slow ramp |
| **W-F4** | **medium, blocking, tooling** | **The `ramp8` corpus can never satisfy the new stale-corpus guard, so S4b, S4c and S4d cannot run out of the box on any platform.** `00f22c55` compares the manifest's `tier` against `Scenario.corpus`; for those three scenarios that field is `"ramp8"`, a *directory* convention, while the corpus is generated as `--tier ramp --scale 8` and its manifest says `"ramp"`. `corpus.py` has no `ramp8` tier to offer (`TIERS` at `corpus.py:205`), so the refusal is unconditional: `corpus … is the 'ramp' tier, this leg needs 'ramp8'`. Worked around here by writing `"tier": "ramp8"` into that one manifest. Fix is a one-liner either way — give the scenario a `corpus_tier` beside its directory name, or teach `corpus.py` the scaled tier names |
| **W-F5** | low, cosmetic | **Seven new `dead_code` warnings on every non-Linux build.** `cgroup_limit_mb`, `cgroup_used_mb`, `bytes_file_mb`, `file_lru_mb`, `FILE_LRU_V1`, `FILE_LRU_V2` and `MIB` in `panoptikon/src/inferio/cpu.rs` are called only from inside `#[cfg(target_os = "linux")]` blocks (`cpu.rs:182`, `cpu.rs:207`) but carry no `cfg` of their own. Windows went 1 warning → 8; macOS will read the same. A `#[cfg(target_os = "linux")]` on the seven items clears it |
| **W-F6** | low, observation | **Throughput is uniformly 3–12 % below run4 with every priced quantity unchanged.** GPU 1, no other tenant: wd-vit 47.2–51.5 items/s against run4's 54.1–55.7; ViT-H-14 97.2–102.0 against 108.6–111.2. GPU 0: S2–S4d 42.1–47.4 against 47.6–50.3. Budgets, anchors, knees, conferred slopes, fitted slopes, headroom and grant counts all match run4 to within a unit or 0.03 %, and there is no OOM, deflation, collapse or retry anywhere, so this is not the ledger. The likely cause is host-side: these impls are CPU-bound in preprocessing, and the user's own instance (8 resident python workers) was up throughout, as it was in run4 — but its load is not something this pass controls. Worth one A/B on an idle host before it is read as a regression |
| **W-F7** | none, method | **`limit_fell` counts scale with sampling density, so the `ledger_invariant` verdict is bistable on the hog legs.** `f-S4c` moved PASS → WARN on **22 of 2 781** GPU-samples with **0 `over_grant`** and 0 of 20 grants over headroom; the same class counts 92 in `f-S4a` (run4: 62), 41 in `f-S4d` (run4: **338**), 25 and 22 in the failbatch legs (run4: 2 and 2). Every one is the hog moving the limit under a footprint already held, which run4 §2 and run5-toobig-2's T5 both recorded as a WARN. The verdict flips on a 0 → non-zero boundary over a leg-length-dependent count; an `over_grant`-weighted threshold would say more |
| **W-A2** | unchanged, reconfirmed | **Token-priced rows still do not travel, and are still unstable within Windows.** `textembed/all-MiniLM-L6-v2` through the OCR chain fits **0.014876** here, **0.014708** in run4 through the same chain and **0.018821** in run4 through the loadgen text route — 0.913 and 1.154 of the Linux 0.016302, straddling it from both sides on one platform. No `aggregation = "max-times-count"` id carries a `platform_copies` copy, and none should |
| **W-A5** | unchanged, still closed | **T7 stays closed.** `f-S14-textembed` ran `doctr/…crnn_mobilenet_v3_small` 300/300 then `textembed/all-MiniLM-L6-v2` over the `text` tier's scanned pages: `grant_safety` PASS, `job_outcome` PASS, 18 fit samples for the embedder |

**Known WDDM residues, named and unchanged.** `oracle_agreement`, `base_accuracy` and
`footprint_agreement` SKIP on every leg **by design**: `selftest.py` reports `base_method
free_delta` ("inferred from a driver delta, not this process") and `oracle: no NVML
per-process figure`, and on WDDM `nvidia-smi --query-compute-apps` answers `[N/A]` too, so
there is no per-process attribution for those three checks to work from. Identical to run4's
§0 verdict line. This is a property of the platform, not of `2133563f`.

## 6. Provenance

| artefact | what |
|---|---|
| `selftest.log`, `selftest-stderr.log`, `win-selftest.json` | §0 |
| `regression/<leg>/` | §2 — 19 legs. `verdicts.json`, `legs.json`, `calibration.after.toml`, `jobs.json`, `failures.json`, `health-t0.json`, `health-end.json`, `runlog.md`, `leg.log` (the driver's stdout) and `ledger-excerpt.log` (the grant / seeding / OOM lines out of `panoptikon.log`), plus `logcounts.json` with the full-log counts the excerpt is cut from |
| `seeded-32gb/<leg>/` | §3 — 6 legs, same file set |
| `toobig/<leg>/` | §4 — 2 legs, same file set; `f-qwen8b-S2-unseeded/ledger-excerpt.log` keeps all 306 ledger lines and the first 3 of the 94 OOM lines (the count is in `logcounts.json`) |
| `copy-usage/copycheck-doctr/` | §4b — `calibration.after.toml`, `loadgen.log`, the health snapshots and the ledger excerpt with the `local=false` seeding line |
| `final-verdicts.json` | every final leg's full `analyze.py` output — verdict, detail and numbers per check |
| `run4-tip-verdicts.json` | run4's 18 legs **re-scored on this tip's `analyze.py`**, the control for §2 |
| `sum-final.json` | the per-leg roll-up the tables are built from (grants, seeding lines, deflation, OOM, store, job) |

**How the two "unseeded" legs were made, and that nothing was left behind.** There is no flag
for "ignore the shipped baseline", so `f-S2-unseeded` and `f-qwen8b-S2-unseeded` ran with
exactly one `[[profile]]` block removed from
`python/inferio/config/calibration/sm_120-linux-cuda.toml` — the `platform = "windows"` block
for that leg's id — from a pristine copy taken first, and restored immediately after. The
counts are in the progress logs: `81` windows rows before the seeded 8B leg, `80` before the
unseeded one, `81` after the restore, `168` profile blocks in the file at the end, and
`git status` on the clone reports the file unmodified.

Drivers used on the box, under the clone's untracked `.win/`: `final-batch1.ps1` /
`final-batch1b.ps1` / `final-batch1c.ps1` (§2), `final-batch2.ps1` / `final-batch2b.ps1` (§3),
`final-batch3.ps1` (§4), `final-batch4.ps1` (§4b), `final-batch5.ps1` (the whisper re-run),
`final-unseed.py`, `final-rescore.ps1`, `final-analyze.ps1`, `final-collect.py`,
`final-sum.py`, `final-pack.ps1`, plus run4's `gw.py`.

**Five legs were run twice, every time through a driver mistake of ours, never the product.**
`f-S14-whisper` first ran without `--scan-audio` (run4's leg passed it), so the scan indexed
180 files instead of 185, the audio never reached the index and the leg reported
`job_no_items`; re-run with the flag it indexed 185 and drained 5/5. The four hogged legs of §3
first ran with the hog on its default device 0 while the gateway was pinned to GPU 1, so no
pressure reached them at all — `share_mb` stayed at ~28 400 MiB under a nominal
`leave-free 8192`, and the hog's own header names `gpu_uuid GPU-b4f312f7…`, the wrong card.
They were re-run with `--hog-device 1`, which is what run4 passed, and only those runs are
reported. A sixth batch was lost before it ran: a PowerShell `foreach ($f in …)` loop in the
first driver clobbered the `$F` holding the output directory (PowerShell variable names are
case-insensitive), so eleven legs failed at their output redirection without starting; they
were re-run intact as batch 1c.

## 7. Wall clock

All times UTC, 2026-09-17.

| task | window | elapsed |
|---|---|---|
| Clone fetched to `2133563f` from a bundle, submodule, `cargo build --release -p panoptikon` (1 m 33 s), `uv sync` (no-op), `pytest` (203 passed) | 00:52 – 00:58 | 6 min |
| `selftest.py` | 00:58 – 00:59 | 1 min |
| Corpus regeneration for `00f22c55`'s generator stamp (5 tiers, incl. the 16 000-item `ramp8`) | 01:05 – 01:08 | 3 min |
| §2 regression, first 7 legs (S1, S2, S3, S4a–S4d) | 01:09 – 01:40 | 31 min |
| §2 regression, remaining 11 legs (5 × S5, 6 × S14) after the driver fix | 01:52 – 02:05 | 13 min |
| run4 re-scored with the tip's `analyze.py` (18 legs) | 02:05 | 6 s |
| `f-S2-unseeded` | 02:05 – 02:07 | 2 min |
| §3 seeded 32 GB set, 6 legs (first pass, hog on the wrong device) | 02:07 – 02:16 | 10 min |
| §4 the two 8B legs | 02:16 – 02:19 | 2 min |
| §4b the doctr copy-usage loadgen leg (600 s of load) | 02:19 – 02:30 | 11 min |
| §3 re-run, the 4 hogged legs with `--hog-device 1` | 02:30 – 02:37 | 7 min |
| `f-S14-whisper` re-run with `--scan-audio` | 02:37 | 1 min |
| `analyze.py` per leg, packing, copy-back | 02:37 – 02:38 | 1 min |
| **Total desktop time** | **00:52 – 02:38** | **1 h 46 min** |

Twenty-eight legs in all: 19 regression, 6 seeded 32 GB, 2 for the 8B, 1 loadgen copy check
(plus 5 legs discarded and re-run, counted in the windows above).

The desktop was left clean: no processes of ours, no listeners on 6379/6382/6383, GPU 1 idle,
the clone back at `0d7f5671` with the calibration file unmodified. The user's own instance on
6339/6342/6343 ran untouched throughout, from before this session started.
