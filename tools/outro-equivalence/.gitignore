# Run outputs: a manifest and two result streams of absolute paths from one
# person's library, regenerated by every run. Regenerate rather than trust a
# stale copy — the sample is deterministic, so a re-run reproduces it.
manifest.tsv
*.jsonl
report.json
__pycache__/
