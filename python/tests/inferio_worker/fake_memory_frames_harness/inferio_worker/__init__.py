"""Fake harness for the orchestrator's per-batch `memory` frame tests.

When this directory is prepended to PYTHONPATH, `python -m inferio_worker`
resolves here instead of the real harness in python/. See __main__.py.
"""
